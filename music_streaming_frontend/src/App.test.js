/**
 * Deprecated default test file. Replaced by src/__tests__/app.smoke.test.jsx
 * Keeping the file but marking a trivial always-pass to avoid CRA warnings
 * if test discovery includes this file.
 */
test('placeholder', () => {
  expect(true).toBe(true);
});
